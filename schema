CREATE TABLE [shelf] (
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT
);

CREATE TABLE [item] (
    id INTEGER NOT NULL PRIMARY KEY,
    idShelf INTEGER,
    name TEXT
);

CREATE TABLE [photo] (
    id INTEGER NOT NULL PRIMARY KEY,
    idItem INTEGER,
    photo TEXT
);
